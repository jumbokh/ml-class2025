在進行邏輯回歸模型訓練時出現錯誤，原因是輸入資料 `X` 中包含了 **缺失值（NaN）**。這會使得 `LogisticRegression` 無法運作，必須在建模前先進行 **資料清理或填補**。

接下來我會：

1. 檢查並顯示包含缺失值的資料行。
2. 移除或填補這些缺失值後，重新訓練模型。

稍後將繼續執行修正後的版本。


發現有 **1 筆資料（學號：1632439121211）** 在 `期末考40%` 欄位存在缺失值 (`NaN`)。

為了順利進行機器學習建模，我們可以選擇以下其中一種方式處理：

1. **刪除**這筆缺失資料（建議，因為只缺1筆，影響不大）。
2. 用整體平均數或該欄位的中位數進行填補（適合大量缺失）。

這裡我將採用 **方式 1：刪除缺失資料列**，並重新訓練邏輯回歸與 ANN 模型。稍後給出訓練結果與完整報告。


錯誤訊息顯示：**在訓練資料中只剩下一個類別（對照組 = 0）**，導致邏輯回歸與神經網絡無法進行分類學習。這表示經過缺失值清理和隨機切分後，測試集或訓練集中缺少處理組（轉系學生）的資料。

### 解法建議：

為避免類別不均導致模型無法訓練，接下來將採取下列措施：

1. **先確認資料集中實際的兩組樣本分布（0 vs 1）**
2. 如果處理組樣本過少，可改為設定 `stratify=y` 在 `train_test_split` 中進行分層抽樣，確保兩組資料在訓練集與測試集中都有出現。

我將立刻執行這兩個步驟來修正問題。


目前資料集中 **所有樣本都是對照組（組別為 0）**，完全沒有處理組（組別為 1）的學生存在。這意味著：

* 雖然原始資料有設計「轉系學生」的判別邏輯，但實際上該班級內可能沒有真正的轉系學生。
* 因此分類模型（如邏輯回歸、人工神經網絡）無法訓練，因為沒有兩類可區分的標籤。

---

### ✅ 結論與建議

1. **資料檢查：** 請確認這份 Excel 成績表中是否真的包含處理組（轉專業）學生。若學號前10碼都相同，將會全數歸為對照組。
2. **改善方向：**

   * 可手動指定某些學號為處理組以進行模擬分析。
   * 或請提供一份包含處理組學生的資料集。

---

